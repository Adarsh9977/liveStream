{
  "name": "mediasoup-server",
  "version": "1.0.0",
  "description": "Mediasoup SFU server for WebRTC to HLS streaming",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc -b",
    "start": "node dist/index.js",
    "dev": "pnpm run build && pnpm run start"
  },
  "dependencies": {
    "@types/ws": "^8.18.1",
    "mediasoup": "^3.12.13",
    "socket.io": "^4.7.2",
    "ws": "^8.18.2"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^20.5.7",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
